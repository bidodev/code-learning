//Conditional, Control flows (if and else)
// 15 - 35 are target demographic

var age = prompt('What is my age');

if ((age >= 18) && (age <= 35)) {
    status = 'target demo'
    console.log(status);

}else{
    status = 'not my audience';
    console.log(status);
}

