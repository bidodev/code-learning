let myFirstCondition = 1 > 4;
let mySecondCond = true;

console.log(myFirstCondition && mySecondCond ? "cool" : "not cool");

// Assigning
let ohNo;
console.log(x);
ohNo = 7;

console.log;

/*
false
0 (zero)
'' or "" (empty string)
null
undefined
NaN

whenever is nothing == false..

Everything else is truthy. That includes:

'0' (a string containing a single zero)
'false' (a string containing the text “false”)
[] (an empty array)
{} (an empty object)
function(){} (an “empty” function)
A single value can therefore be used within conditions, e.g.

*/
