const hello = name => {
  console.log(`Hello ${name}`);
};

const MY_NAME = "Claudinei";
hello(MY_NAME);

/**
 * Node Module System
 *
 */
//Global objects JavaScript
// console.log;
// clearTimeout();
// setInterval();
// setTimeout();
// clearInterval();

const message = "My Message";
/**
 * The variables declared inside this file are only avaible for it owns scope. They're not added to the global object.
 */
